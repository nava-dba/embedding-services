ui:
  image: icr.io/ai-services-cicd/catalog-ui:v0.0.69
  resources:
    requests:
      memory: "512Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"

backend:
  image: icr.io/ai-services-cicd/ai-services:v0.0.349
  runtime: "openshift"
  adminPasswordHash: ""
  # @generate:password length=32, special=false
  dbEncryptionKey: ""
  # workerGatewayPort: port for the gRPC worker gateway. Always active; default is 9090.
  workerGatewayPort: "9090"
  # localWorker: when "true", the catalog-backend auto-joins a co-located worker pod.
  localWorker: "true"
  # @generate:password length=32, special=false
  mtlsEncryptionKey: ""
  resources:
    requests:
      memory: "1Gi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "1000m"

bundles:
  storage: 5Gi

db:
  image: icr.io/ai-services-cicd/postgres:18-5
  user: "postgres"
  database: "ai_services"
  password: ""
  storage: 5Gi
  resources:
    requests:
      memory: "512Mi"
      cpu: "100m"
    limits:
      memory: "1Gi"
      cpu: "500m"
