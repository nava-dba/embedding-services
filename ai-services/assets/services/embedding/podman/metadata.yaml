name: similarity
version: "1.0.0"

# Resource requirements
resources:
  cpu: 1 # 1 core (rounded up from 500m in OpenShift template)
  memory: 1073741824 # 1Gi in bytes based on podman template limits
  storage: 0 # No persistent storage required


# Made with Bob
